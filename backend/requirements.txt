# Python Backend Requirements for INDAS Exhibition Lead Capture System
# Tested with Python 3.10+

# SYSTEM DEPENDENCIES REQUIRED:
# 1. ODBC Driver 17 for SQL Server
#    Linux: sudo apt install unixodbc unixodbc-dev
#    Windows/Mac: Download from Microsoft

# Web Framework
fastapi>=0.121.0
uvicorn[standard]>=0.38.0
python-multipart>=0.0.20

# Database
pyodbc>=5.3.0

# AI & ML
openai>=2.8.0

# Image Processing & OCR (Core - Required)
opencv-python-headless>=4.12.0
easyocr>=1.7.0
Pillow>=12.0.0
numpy>=2.2.0,<2.3.0

# Optional Features (can be commented out if not needed)
# QR Code Scanner - Requires system library 'libzbar'
#   Linux: sudo apt install libzbar0
#   Mac: brew install zbar
#   Windows: Download from https://sourceforge.net/projects/zbar/
# pyzbar>=0.1.9

# Audio Processing - Only needed for WhatsApp voice notes (not web voice)
# Requires FFmpeg system binary
#   Linux: sudo apt install ffmpeg
#   Mac: brew install ffmpeg
#   Windows: choco install ffmpeg
# ffmpeg-python>=0.2.0

# Utilities
python-dotenv>=1.2.0
pydantic>=2.12.0
pydantic-settings>=2.12.0
email-validator>=2.0.0

# Authentication & Security
python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4

# HTTP Client (for WhatsApp API calls)
httpx>=0.28.0
requests>=2.32.0

# Scheduler for automated tasks
APScheduler>=3.10.0

# NLP & Date Parsing
dateparser>=1.2.0
python-dateutil>=2.8.2

# Testing (Optional)
pytest>=7.4.0,<8.0.0
pytest-asyncio>=0.23.0
